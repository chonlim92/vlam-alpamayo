# VLAM-Alpamayo — Core Dependencies
# Install: pip install -r requirements.txt

# App
python-dotenv>=1.0.0
gradio>=5.0.0

# PyTorch (pinned to Alpamayo-required versions)
torch==2.8.0
torchvision>=0.23.0
transformers==4.57.1

# Hugging Face
huggingface-hub>=0.30.0
datasets>=3.0.0
accelerate>=1.12.0

# Alpamayo runtime deps (covers both v1 and v1.5)
einops>=0.8.1
hydra-core>=1.3.2
hydra-colorlog>=1.2.0
rich>=14.0.0
mediapy>=1.2.4
wandb>=0.25.0
torchmetrics>=1.8.0
deepspeed>=0.17.4

# Scientific / visualization
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.3.3
matplotlib>=3.10.0
seaborn>=0.13.2
pillow>=12.0.0
opencv-python>=4.8.0
av>=16.0.1

# ──────────────────────────────────────────────────────────────
# After installing the above, run these separately:
#
#   pip install flash-attn --no-build-isolation
#   pip install --no-deps git+https://github.com/NVlabs/alpamayo.git
#   pip install --no-deps git+https://github.com/NVlabs/alpamayo1.5.git
#
# flash-attn needs torch at build time (--no-build-isolation).
# Alpamayo packages are installed with --no-deps because their
# pyproject.toml pulls in packages not on PyPI (physical-ai-av,
# cosmos-rl). All actual runtime deps are covered above.
# ──────────────────────────────────────────────────────────────
