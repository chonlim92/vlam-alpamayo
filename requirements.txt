# Core dependencies
python-dotenv>=1.0.0
torch>=2.8.0
transformers>=4.57.1
huggingface-hub>=0.30.0
datasets>=3.0.0
deepspeed>=0.17.4
gradio>=5.0.0
opencv-python>=4.8.0
numpy>=1.26.0

# Alpamayo model packages (installed from GitHub)
# pip install git+https://github.com/NVlabs/alpamayo.git
# pip install git+https://github.com/NVlabs/alpamayo1.5.git

# NVIDIA PhysicalAI AV dataset toolkit
physical-ai-av>=0.1.0

# Flash Attention 2 (optional — requires CUDA Toolkit with nvcc)
# pip install flash-attn --no-build-isolation
